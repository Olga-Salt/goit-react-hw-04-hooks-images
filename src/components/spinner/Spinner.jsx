import React from 'react';
import { Bars } from 'react-loader-spinner';

function Spinner() {
  return <Bars color="#00BFFF" height={80} width={80} arialLabel="loading" />;
}

export default Spinner;
